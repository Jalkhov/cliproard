{
  "$schema": "https://raw.githubusercontent.com/googleapis/release-please/main/schemas/config.json",
  "release-type": "simple",
  "extra-files": ["src/manifest.json", "package.json"],
  "pull-request-header": ":robot: Preparing launch of ${component} ${version}"
}
